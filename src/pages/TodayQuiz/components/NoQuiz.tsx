import { NoQuizContainer } from '../styles';

export default function NoQuiz() {
    return (
        <NoQuizContainer>
            <span>
                아직 우려낼 지식이 없어요<b>+버튼으로 새로운 지식 추가하기</b>
            </span>
        </NoQuizContainer>
    );
}
